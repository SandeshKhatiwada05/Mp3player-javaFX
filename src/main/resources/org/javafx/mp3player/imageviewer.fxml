<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.geometry.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="org.javafx.mp3player.ImageViewerController"
            stylesheets="@style.css">

    <top>
        <Label text="🖼 Sandesh's Image Viewer"
               styleClass="title-label"
               BorderPane.alignment="CENTER"/>
    </top>

    <center>
        <VBox alignment="CENTER" spacing="15" BorderPane.alignment="CENTER">
            <ImageView fx:id="imageView"
                       fitHeight="400"
                       fitWidth="600"
                       preserveRatio="true"
                       style="-fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.5), 10, 0, 0, 5);"/>
            <Label fx:id="imageLabel"
                   text="No image selected"
                   styleClass="song-label"/>
        </VBox>
    </center>

    <bottom>
        <HBox alignment="CENTER" spacing="15">
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>
            <Button text="📂 Choose Image" onAction="#chooseImage" styleClass="action-button"/>
            <Button text="⬅ Back" onAction="#goBack" styleClass="back-button"/>
        </HBox>
    </bottom>
</BorderPane>
